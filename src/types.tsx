export type Product = {
  id: string
  name: string
  imageURL: string
  price: number | null
  size: string | number | undefined
  tags: string[]
  status: string
}

export type Favs = {
  id: string
  name: string
  imageURL: string
  price: number | null
  size: string | number | undefined
  tags: string[]
  status: string
}
