export type Product = {
  id: string
  name: string
  imageURL: string
  price: number | null
  size: number | undefined
  status: string
}

export type Favs = {
  id: string
  name: string
  imageURL: string
  price: number | null
  size: number | undefined
  status: string
}
